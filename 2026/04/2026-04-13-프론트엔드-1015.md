# React에서 상태 관리 라이브러리인 Redux를 사용할 때, Redux의 기본 개념과 React와의 통합 방식에 대해 설명해 주세요.

> **카테고리**: 프론트엔드 | **난이도**: 중 | **점수**: 85/100

---

## 내 답변

Redux is a state management library for JavaScript apps. It uses a single store to hold the entire application state. Actions are dispatched to describe state changes, and reducers are pure functions that take the previous state and an action to return a new state. This unidirectional data flow makes state changes predictable and debugging easier with Redux DevTools.

---

## AI 피드백

### 내용 완성도
답변에서 Redux의 기본 개념을 잘 설명했으나, React와의 통합 방식에 대한 설명이 부족합니다. React에서 Redux를 어떻게 사용하는지에 대한 구체적인 예시가 필요합니다.

### 답변 구조
답변이 명확하고 논리적으로 구성되어 있으나, 추가적인 세부정보를 포함하여 더 잘 정리할 수 있습니다.

### 표현/말투
전반적으로 전문적인 표현을 사용하였으나, 좀 더 간결하게 설명할 수 있는 부분이 있습니다.

### 구체성
Redux의 개념을 잘 설명했으나, React와의 통합 방식에 대한 구체적인 세부사항이 부족합니다. 예를 들어, 'Provider'와 'connect' 혹은 'useSelector'와 'useDispatch' 훅에 대한 언급이 필요합니다.

---

## 모범 답변

Redux는 JavaScript 애플리케이션을 위한 상태 관리 라이브러리입니다. 애플리케이션의 전체 상태를 보유하는 단일 스토어를 사용합니다. 상태 변경을 설명하기 위해 액션이 디스패치되며, 리듀서는 이전 상태와 액션을 받아 새로운 상태를 반환하는 순수 함수입니다. 이와 같은 단방향 데이터 흐름은 상태 변경을 예측 가능하게 하여 Redux DevTools로 디버깅을 쉽게 만듭니다. React와 통합할 때는 'react-redux' 라이브러리를 사용하여, 'Provider' 컴포넌트로 애플리케이션을 감싸고, 'useSelector' 훅으로 상태를 가져오고, 'useDispatch' 훅으로 액션을 디스패치하는 방식으로 사용합니다.
